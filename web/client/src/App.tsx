// Design system: Quiet Field Instrument — persistent browser shell, editorial typography, dark plum surfaces, signal amber actions.
import Home from "@/pages/Home";

export default function App() {
  return <Home />;
}
