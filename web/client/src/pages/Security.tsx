import { ArrowLeft, ArrowUpRight, KeyRound, ScanSearch, ShieldCheck } from "lucide-react";
import { Link } from "wouter";
import { LyconFooter, LyconHeader } from "../components/LyconFrame";

/** Lycon Browser: security documentation makes posture sensitivity and egress control legible to browser users. */
export default function Security() {
  return <main className="trailhead-shell"><div className="ambient ambient-two" /><div className="trailhead-page product-page"><LyconHeader /><section className="doc-hero"><Link className="back-link" href="/"><ArrowLeft size={14} /> Back to browser</Link><span className="subpage-kicker">Documentation / security</span><h1>Stay alert.<br /><em>Stay in control.</em></h1><p>Hunter’s Posture lets you decide how sensitive Lycon should be, while every intelligence handoff remains explicit and auditable.</p></section><section className="doc-grid"><article><ShieldCheck size={20} /><h2>Hunter’s Posture</h2><p>Hardened limits remote context egress, Balanced provides practical defaults, and Permissive is available when you intentionally need broader connectivity.</p></article><article><ScanSearch size={20} /><h2>Explicit egress</h2><p>Requests name their context and destination before they happen. Lycon does not silently send page, selection, or local-file context to a connector.</p></article><article><KeyRound size={20} /><h2>Protected credentials</h2><p>Native builds use platform-backed protection: Windows Credential Locker and Android Keystore-backed encryption for connector secrets.</p></article></section><div className="doc-callout"><strong>Review the architecture</strong><span>The posture model, transport behavior, and audit boundaries are maintained in the source repository.</span><a href="https://github.com/Daemon22/Lycon" target="_blank" rel="noreferrer">Open architecture source <ArrowUpRight size={14} /></a></div><LyconFooter /></div></main>;
}
