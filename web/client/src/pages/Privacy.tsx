import { ArrowLeft, ArrowUpRight, Database, EyeOff, FileLock2 } from "lucide-react";
import { Link } from "wouter";
import { LyconFooter, LyconHeader } from "../components/LyconFrame";

/** Lycon Browser: privacy documentation describes the product boundary without promising native behavior on the web. */
export default function Privacy() {
  return <main className="trailhead-shell"><div className="ambient ambient-one" /><div className="trailhead-page product-page"><LyconHeader /><section className="doc-hero"><Link className="back-link" href="/"><ArrowLeft size={14} /> Back to browser</Link><span className="subpage-kicker">Documentation / privacy</span><h1>Your trail<br /><em>stays yours.</em></h1><p>Lycon is designed around a simple boundary: browsing should remain useful without making your activity, local files, or credentials a product.</p></section><section className="doc-grid"><article><EyeOff size={20} /><h2>No silent intelligence</h2><p>Lycon does not invoke an agent in the background. Context and destination are chosen explicitly before an intelligence request leaves the device.</p></article><article><FileLock2 size={20} /><h2>Local means local</h2><p>Native file paths, local content, and protected credentials stay in the native application boundary. This online page does not request filesystem or credential access.</p></article><article><Database size={20} /><h2>Minimal metadata</h2><p>Auditable request records describe what happened without storing page contents or secrets. See the source implementation for the current retention behavior.</p></article></section><div className="doc-callout"><strong>Read the implementation</strong><span>Privacy behavior is documented alongside the browser source and release notes.</span><a href="https://github.com/Daemon22/Lycon" target="_blank" rel="noreferrer">Open GitHub <ArrowUpRight size={14} /></a></div><LyconFooter /></div></main>;
}
