# Legacy tests

These files were moved here as part of the legacy cleanup.

They correspond to the previous Electron/browser automation harness and are not used by the current app or active CI/test scripts. The repository now runs its live checks through the root `vitest` setup instead.

This archive is kept only for historical reference and debugging older behavior.
